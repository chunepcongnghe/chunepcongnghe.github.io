(()=>{const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];const menu=q('[data-menu]'),mobile=q('[data-mobile-nav]');if(menu&&mobile){menu.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))})}const search=q('[data-site-search]');if(search){search.addEventListener('input',e=>{const term=e.target.value.trim().toLowerCase();qa('[data-search-item]').forEach(el=>{const hay=(el.dataset.search||el.innerText).toLowerCase();el.classList.toggle('search-hidden',term&&!hay.includes(term))})})}qa('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const value=btn.dataset.filter;qa('[data-filter]').forEach(b=>b.classList.toggle('active',b===btn));qa('[data-category]').forEach(card=>card.classList.toggle('search-hidden',value!=='all'&&card.dataset.category!==value))}));const back=q('[data-backtop]');if(back){const sync=()=>back.classList.toggle('show',scrollY>600);addEventListener('scroll',sync,{passive:true});sync();back.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}))}const content=q('.article-content'),toc=q('[data-toc]');if(content&&toc){const hs=qa('h2,h3',content);if(hs.length){hs.forEach((h,i)=>{if(!h.id)h.id='muc-'+(i+1);const a=document.createElement('a');a.href='#'+h.id;a.textContent=h.textContent;a.style.paddingLeft=h.tagName==='H3'?'12px':'0';toc.appendChild(a)})}else toc.closest('.toc')?.remove()}async function loadChuNep(){const targets=qa('img[data-chunep]');if(!targets.length)return;try{const urls=Array.from({length:7},(_,i)=>`/image_parts/chunep.part${String(i+1).padStart(2,'0')}.txt?v=3`);const parts=await Promise.all(urls.map(async u=>{const r=await fetch(u,{cache:'force-cache'});if(!r.ok)throw new Error(u);return(await r.text()).replace(/\s+/g,'')}));const src='data:image/webp;base64,'+parts.join('');targets.forEach(img=>{img.src=src;img.removeAttribute('data-chunep')})}catch(err){console.error('Chú Nếp image load failed',err);targets.forEach(img=>{img.src='/assets/chunep-canonical.webp';img.removeAttribute('data-chunep')})}}loadChuNep();})();